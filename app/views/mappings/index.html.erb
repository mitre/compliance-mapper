<p id="notice">
    <%= notice %>
</p>
<h1>Mappings</h1>
<br/>
<% y = 'test' %>
<div class="input-group mb-3">
    <div class="input-group-prepend">
        <button type="button" class="btn btn-outline-secondary">Generate Mappings</button>
        <button type="button" class="btn btn-outline-secondary dropdown-toggle dropdown-toggle-split" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <span class="sr-only">Toggle Dropdown</span>
        </button>
        <div class="dropdown-menu">
            <% x = Control.all.each do |control| %>
              <!-- <a class="dropdown-item" href="#">Action<%= x %></a> -->
              <a class="dropdown-item" href="#"><%=control.title %></a>
              <%  %>
            <% end %>
        </div>
    </div>
    <input type="text" class="form-control" aria-label="Text input with segmented dropdown button">
</div>
<div id="mappings">
      <% control = Nistcontrol.find(Nistcontrol.where(number: "AC-2").ids[0]) %>
      <%= y %>
</div>


<!-- <table>
    <thead>
        <tr>
            <th colspan="3"></th>
        </tr>
    </thead>
    <tbody>
        <% @mappings.each do |mapping| %>
            <tr>
                <td>
                    <%= link_to 'Show', mapping %>
                </td>
                <td>
                    <%= link_to 'Edit', edit_mapping_path(mapping) %>
                </td>
                <td>
                    <%= link_to 'Destroy', mapping, method: :delete, data: { confirm: 'Are you sure?' } %>
                </td>
            </tr>
            <% end %>
    </tbody>
</table> -->
<br>
<%= link_to 'New Mapping', new_mapping_path %>